# Sprint Scope

One Markdown file per sprint. Generated by the `backlog-manager` agent from Jira/ADO sprint data.

Files:

* No sprints yet.

Rules:

* One flat file per sprint: `sprint-N.md`.
* Do not create subfolders per sprint.
* Sprint scope files are durable records of what was planned vs delivered.
* The `backlog-manager` agent also generates sprint scope emails from these files.
